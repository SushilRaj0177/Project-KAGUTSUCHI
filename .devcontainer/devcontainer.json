{
  "name": "kagutsuchi-backend",
  "image": "mcr.microsoft.com/devcontainers/universal:2",
  "forwardPorts": [8000],
  "portsAttributes": {
    "8000": {
      "label": "KAGUTSUCHI API",
      "onAutoForward": "notify"
    }
  },
  "postCreateCommand": "python3 -m pip install -r server/requirements.txt -r requirements.txt -r verification/requirements.txt",
  "remoteEnv": {
    "PYTHONPATH": "${containerWorkspaceFolder}"
  }
}
